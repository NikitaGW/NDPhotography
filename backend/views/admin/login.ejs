<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        * {
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            margin: 0;
            height: 100vh;
            background: linear-gradient(to right, #3b2d7d, #4420a1);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }

        .login-container {
            display: flex;
            background-color: transparent;
            width: 80%;
            max-width: 1000px;
            justify-content: space-between;
        }

        .info {
            flex: 1;
            padding: 40px;
        }

        .info h2 {
            font-size: 36px;
            margin-bottom: 20px;
        }

        .info p {
            font-size: 17px;
            line-height: 1.6;
        }

        .form-container {
            flex: 1;
            background-color: #ffffff;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            color: #333;
        }

        .form-container h3 {
            margin-bottom: 20px;
            font-size: 24px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            font-weight: 600;
            display: block;
            margin-bottom: 8px;
        }

        input[type="text"], input[type="password"] {
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 14px;
        }

        .btn-login {
            background-color: #ff0080;
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn-login:hover {
            background-color: #e60073;
        }

        @media (max-width: 768px) {
            .login-container {
                flex-direction: column;
                align-items: center;
            }

            .info, .form-container {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="info">
            <h2>LogIn Of</h2>
            <p>
                This system is designed by A2Z INFOTECH company on 2021.<br>
                If you have any query about system/software you can contact us on<br>
                Call / WhatsApp +91 9011144920<br>
                Visit Our Website <a href="https://a2zinfotechs.com" style="color: #ffb6ff;" target="_blank">a2zinfotechs.com</a>
            </p>
        </div>
        <div class="form-container">
            <form action="/admin/login_process" method="POST">
                <div class="form-group">
                    <label for="username">Username</label>
                    <input type="text" id="username" name="username" placeholder="your email" required>
                    <small>Your unique username to app</small>
                </div>

                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="password" required>
                    <small>Your password</small>
                </div>

                <button class="btn-login" type="submit">Secure login</button>
            </form>
        </div>
    </div>
</body>
</html>
